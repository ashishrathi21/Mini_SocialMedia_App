<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <style>
      .no-scrollbar::-webkit-scrollbar {
        display: none;
      }
    </style>
  </head>

  <body class="bg-black overflow-hidden">
    <div
      class="h-screen max-w-sm w-full mx-auto bg-zinc-900 text-white rounded-lg shadow-lg flex flex-col"
    >
      <!-- HEADER -->
      <header class="p-4 text-center flex justify-center">
        <div class="text-xl md:text-2xl font-bold">Mini Social Media App</div>
      </header>

      <!-- PROFILE SECTION -->
      <div class="p-4 space-y-6">
        <!-- User Info Row -->
        <div
          class="flex justify-between items-center bg-zinc-800 p-4 rounded-xl"
        >
          <!-- Left text -->
          <div class="space-y-1">
            <p class="text-lg font-semibold"><%= user.name %></p>
            <p class="text-zinc-400 text-sm">@<%= user.username %></p>
            <p class="text-zinc-500 text-sm"><%= user.email %></p>
          </div>

          <!-- Right profile image -->
          <img
            src="https://cdn.pixabay.com/photo/2015/10/05/22/37/blank-profile-picture-973460_640.png"
            class="w-25 h-25 rounded-lg object-cover border border-zinc-700"
            alt=""
          />
        </div>

        <!-- Create Post Section -->
        <div class="bg-zinc-800 p-4 rounded-xl">
          <div class="flex items-center space-x-3">
            <i class="fa-solid fa-circle-plus text-2xl text-blue-400"></i>
            <p class="text-lg font-medium">Create New Post</p>
          </div>

          <a href="/post/create">
            <button
            class="mt-4 w-full bg-blue-600 hover:bg-blue-500 py-2 rounded-lg font-semibold"
          >
            Create
          </button>
          </a>
        </div>

        <div>
          <button
            class="mt-4 w-full bg-blue-600 hover:bg-blue-500 py-2 rounded-lg font-semibold"
          >
            View Your Posts
          </button>
        </div>

        <div>
          <a href="/logout">
            <button
            class="mt-4 w-full bg-red-400 hover:bg-red-500 py-2 rounded-lg font-semibold"
          >
            Logout
          </button>
          </a>
        </div>
      </div>

      <!-- Bottom Nav -->
      <nav class="p-4 rounded-b-lg mt-auto">
        <ul class="flex justify-around text-lg">
          <a href="/post">
            <li
            class="cursor-pointer text-zinc-300 bg-zinc-800 px-8 py-2 rounded-lg"
          >
            Posts
          </li>
          </a>
          <a href="/account">
            <li
            class="cursor-pointer text-blue-300 bg-zinc-800 px-8 py-2 rounded-lg"
          >
            Account
          </li>
          </a>
        </ul>
      </nav>
    </div>
  </body>
</html>
